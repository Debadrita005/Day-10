#include<stdio.h>
#include<limits.h>
int main()
int max=INT-MIN;
int find Min(int arr[],int size)
{
	for (int i=0;i<size;i++)
	{
		if (arr[i]<min\min=arr[i])
	}
	return min;
}
int find Max(int arr[],int size)
{
	for (int i=0;i<size;i++)
	{
		if (arr[i]>max)max=arr[i]
	}
	return max;
}
